node_modules
.next
.venv
__pycache__
out
.env
.env.local
.env*.local
.DS_Store
*.log
.vscode
.idea

# Internal-only working docs (security notes, technical debt log, etc).
# These contain sensitive context and explicit references to past security
# issues — never commit, never share publicly.
INTERNAL-NOTES.md
